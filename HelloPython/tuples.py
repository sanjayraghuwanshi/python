number = [1, 2, 3] #list
'''
tuples - can't add/change anything to tuples
We can only print the stuff from tuples
If you don't want the item from the list to
be modified anywhere then you can prefer to 
use tuples instead of lists
'''
numbers = (1, 2, 3)
print(numbers[0])       #prints the first item